# Simple Database (C#)

A minimal database implementation written in C#.  
This project follows the "Let's Build a Simple Database" tutorial, but implemented in C# instead of C.

## Goals

- Understand database internals
- Implement B-Tree storage
- Build a pager
- Create a simple virtual machine
